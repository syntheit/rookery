import { HeaderNavbar } from "~/app/_components/header-navbar";

export default function Page() {
    return (
        <div>
            <HeaderNavbar></HeaderNavbar>
            ratings page, displays registered player ratings in ascending order
        </div>
    );
  }